9 new features. 73 bug fixes. 14 improvements. The biggest shifts land on agent management, goal-driven runs, and auto-mode safety.

First. The new agents view lists every Claude Code session you have. Running, blocked, or done. Run claude agents to see them all in one place.

Second. The slash goal command sets a completion condition. Claude keeps working across turns until it is met. Elapsed time, turn count, and tokens update as it runs.

Third. The settings dot auto mode dot hard deny rule blocks actions unconditionally. The classifier can no longer override it. The absolute no you have been waiting for.

Fourth. Transcript view finally has keyboard navigation. Question mark for shortcuts, curly braces to jump between user prompts, and v to toggle the panel.

Fifth. Claude plugin details shows a plugin's components and its projected token cost. The price tag before you install.

Sixth. Multi server M C P setups no longer lose O auth tokens during concurrent refreshes. The daily re auth dance is over.

Seventh. The compaction prompt now asks the model to preserve your instructions. Long sessions no longer lose user intent when context gets trimmed.

Eighth. The deadlock that blocked claude auth login, logout, and status is patched. Expired credentials no longer hang the entire C L I.

Ninth. Slash M C P reconnect now picks up dot mcp dot json edits live. No restart needed when you tweak server config.

Which patch matters most. Agent view. Slash goal. Or hard deny. Drop a comment.

If you want to learn more about AI, check out the dynamous dot ai community.
