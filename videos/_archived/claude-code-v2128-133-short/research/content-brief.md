# Content Brief — Claude Code v2.1.128 → v2.1.133 — Short

## Version Range
v2.1.128, v2.1.129, v2.1.131, v2.1.132, v2.1.133  (v2.1.127 + v2.1.130 absent from upstream changelog)

Slug: `claude-code-v2128-133-short`
Render target: 1080x1920 vertical, 30fps, 70-130s.

## Reused-from
`videos/claude-code-v2128-133/` (long-form spawned 2026-05-08). Stats and highlights copied verbatim from its `research/content-brief.md`.

## Stats (Phase 2 — three pills)

- **Features:** 28
- **Fixes:** 78
- **Improved:** 4

## Top 3 Highlights (Phase 3 — numbered cards)

Picked from the long-form's user-confirmed 6 — strongest 3 for a Short:

1. `$CLAUDE_EFFORT` in hooks + Bash (v133) — hooks now know which effort level is running, Bash subprocesses can read `$CLAUDE_EFFORT`. Most novel + actionable for power users.
2. `--plugin-url <url>` flag (v129) — fetch a plugin `.zip` from any URL for the current session. Zero install, immediate utility.
3. Subagent skills discovery fix (v133) — subagents now find project, user, and plugin skills via the Skill tool again. Regression patched, broadly relevant.

## Hook Angle

Six Claude Code releases in one week. The headline: hooks just got smart, plugins install from a URL, and subagents finally find skills again.

## Caption Pill Text

"Six releases in one week"

## Phase 1 Slam

Header overline: "CLAUDE CODE UPDATE"
Pre-line: "Versions"
Slam range: `v2.1.128 → v2.1.133`  (note: range not single tag — see don'ts in playbook re: tight slam budget; we may compress to `v2.1.133` if overflow)
Caption pill: "Six releases in one week"

## Phase 4 CTA

Overline: "PULL EVERY CHANGE"
Terminal: `$ claude update`
Subscribe pill: "Subscribe"
Debate question: "Which patch made your day — hooks, plugins, or skills?"

## WatchNext

Skipped (matches long-form choice).
