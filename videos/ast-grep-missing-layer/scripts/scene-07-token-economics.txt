Let's translate that into money. To audit those 22 files, your agent has to open 22 files. That's roughly eleven thousand tokens of context — call it a cent for a single refactor. Now scale it up. If your team runs fifty refactors a day, thirty days a month, that cent becomes fifteen dollars of pure waste, per developer, just on bad search. ast-grep does the same job in one structured query. Roughly half the tokens. Your agent stops re-reading files it shouldn't have opened.
